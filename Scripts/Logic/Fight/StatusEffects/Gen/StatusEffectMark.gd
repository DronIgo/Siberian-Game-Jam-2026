# This file is autogenerated

class_name StatusEffectMark
extends StatusEffectBase

# constants from config
const default_duration : int = 2
const tags : Array = ["negative"]
const amount : int = 80

func _init(duration : int = default_duration) -> void:
	lore_name = "метка"
	lore_description_template = "Получает на {amount} процентов больше урона от всех источников"
	type = StatusGenerator.STATUS.MARK
	super(duration)

func get_description() -> String:
	var format_dict : Dictionary = {}
	format_dict["amount"] = amount

	return lore_description_template.format(format_dict)

func on_turn_end(actor : ActorBase, data = null) -> void:
	duration -= 1

func on_turn_start(actor : ActorBase, data = null) -> void:
	pass
