# This file is autogenerated

class_name StatusEffectAmnezia
extends StatusEffectBase

# constants from config
const default_duration : int = 3
const tags : Array = []

var removed_action : ActionBase

func _init(duration_ : int = default_duration) -> void:
	lore_name = "амнезия"
	lore_description_template = "Забыл как использовать {removed_action.lore_name}"
	type = StatusGenerator.STATUS.AMNEZIA
	_tags = tags
	super(duration_)

func init(target : ActorBase) -> void:
	var actions = target.action_holder.get_actions()
	var rand_action_idx = randi_range(0, actions.size())
	removed_action = actions[rand_action_idx]

func get_description() -> String:
	var format_dict : Dictionary = {}
	format_dict["removed_action.lore_name"] = removed_action.lore_name

	return lore_description_template.format(format_dict)

func on_turn_end(actor : ActorBase, data = null) -> void:
	duration -= 1

func on_turn_start(actor : ActorBase, data = null) -> void:
	pass
