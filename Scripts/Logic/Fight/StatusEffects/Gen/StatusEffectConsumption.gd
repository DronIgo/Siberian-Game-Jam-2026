# This file is autogenerated

class_name StatusEffectConsumption
extends StatusEffectBase

# constants from config
const default_duration : int = 3
const tags : Array = []
const amount : int = 5

func _init(duration_ : int = default_duration) -> void:
	lore_name = "поглощение"
	lore_description_template = "Восстанавливает {amount} здоровья при получении урона"
	type = StatusGenerator.STATUS.CONSUMPTION
	_tags = tags
	super(duration_)

func get_description() -> String:
	var format_dict : Dictionary = {}
	format_dict["amount"] = amount

	return lore_description_template.format(format_dict)

func on_turn_end(actor : ActorBase, data = null) -> void:
	duration -= 1

func on_turn_start(actor : ActorBase, data = null) -> void:
	pass

func on_damage_taken(actor : ActorBase, damage : int) -> void:
	##TAKE_DAMAGE START
	actor.heal(amount)
	##TAKE_DAMAGE END
