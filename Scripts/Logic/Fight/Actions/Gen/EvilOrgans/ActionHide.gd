# This file is autogenerated

class_name ActionHide
extends ActionBase

# constants from config
const manacost : int = 0

func _init() -> void:
	lore_name = "Прятки"
	description = "Даёт 100% брони на 1 ход"
	result_format = "{initiator.lore_name} прячется, получая 100% брони"
	_manacost = manacost

func get_priority(actor : ActorBase, own : OrganBase) -> int:

	if own == actor:
		return 3

	if (actor is OrganBase and own.is_healthy == actor.is_healthy):
		return 2

	return -1

func take_action(initiator: ActorBase, targets : Array) -> ActionResult:
	super.take_action(initiator, targets)

	##EFFECTS START
	targets[0].apply_status(SEG.create_status(StatusGenerator.STATUS.HIDE))
	##EFFECTS END

	var format_dict : Dictionary = {}
	format_dict["initiator.lore_name"] = initiator.lore_name

	return ActionResult.new(result_format, format_dict)
