# This file is autogenerated

class_name ActionScanVul
extends ActionBase

# constants from config
const manacost : int = 0

func _init() -> void:
	code_name = "scan_vul"
	lore_name = "Поиск уязвимости"
	description = "Увеличивает свой урон"
	result_format = "{initiator.lore_name} ищет уязвимости, увеличивая свой урон"
	usage_sound_name = "res://Assets/SFX/fly_find_vulnerability.mp3"
	_manacost = manacost

func get_priority(actor : ActorBase, own : OrganBase) -> int:

	if own == actor:
		return 3

	return -1

func take_action(initiator: ActorBase, targets : Array) -> ActionResult:
	super.take_action(initiator, targets)

	##EFFECTS START
	targets[0].apply_status(SEG.create_status(StatusGenerator.STATUS.BUFF_ATTACK))
	##EFFECTS END

	var format_dict : Dictionary = {}
	format_dict["initiator.lore_name"] = initiator.lore_name

	return ActionResult.new(result_format, format_dict)
