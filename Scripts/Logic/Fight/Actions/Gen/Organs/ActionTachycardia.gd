# This file is autogenerated

class_name ActionTachycardia
extends ActionBase

##CONST START
# constants from config
const manacost : int = 0
##CONST END

func _init() -> void:
	code_name = "tachycardia"
	lore_name = "Тахикардия"
	description = "Выбранная цель немедленно совершает ещё один ход"
	result_format = "{initiator.lore_name} дает {target.lore_name} совершить ещё один ход"
	usage_sound_name = "res://Assets/SFX/heart_tachycardia.mp3"
	_manacost = manacost

func get_priority(actor : ActorBase, own : OrganBase) -> int:

	if (actor is OrganBase and own.is_healthy == actor.is_healthy and own != actor):
		return 3

	return -1

func take_action(initiator: ActorBase, targets : Array) -> ActionResult:
	super.take_action(initiator, targets)
	var target: ActorBase = targets[0]

	##EFFECTS START
	##EFFECTS END
	FightEventBus.add_actor_turn.emit(target)
	

	var format_dict : Dictionary = {}
	format_dict["target.lore_name"] = targets[0].lore_name
	format_dict["initiator.lore_name"] = initiator.lore_name

	return ActionResult.new(result_format, format_dict)
