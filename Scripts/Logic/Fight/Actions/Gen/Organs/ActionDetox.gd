# This file is autogenerated

class_name ActionDetox
extends ActionBase

# constants from config
const manacost : int = 0

func _init() -> void:
	code_name = "detox"
	lore_name = "Детоксикация"
	description = "Снимает негативные эффекты"
	result_format = "{initiator.lore_name} снимает все негативные эффекты"
	usage_sound_name = "res://Assets/SFX/liver_detox.mp3"
	_manacost = manacost

func get_priority(actor : ActorBase, own : OrganBase) -> int:

	if own == actor:
		return 3

	return -1

func take_action(initiator: ActorBase, targets : Array) -> ActionResult:
	super.take_action(initiator, targets)

	##EFFECTS START
	##EFFECTS END

	var format_dict : Dictionary = {}
	format_dict["initiator.lore_name"] = initiator.lore_name

	return ActionResult.new(result_format, format_dict)
