# This file is autogenerated

class_name ActionStudy
extends ActionBase

##CONST START
# constants from config
const manacost : int = 5
##CONST END

func _init() -> void:
	code_name = "study"
	lore_name = "Анализ"
	description = "Вы получаете случайную способность выбранного органа"
	result_format = "Вы получили способность {action.lore_name}"
	usage_sound_name = "res://Assets/SFX/firefly_light.mp3"
	_manacost = manacost

func get_priority(actor : ActorBase, own : OrganBase) -> int:

	if (actor is OrganBase and own.is_healthy != actor.is_healthy):
		return 3

	return -1

func take_action(initiator: ActorBase, targets : Array) -> ActionResult:
	super.take_action(initiator, targets)
	var target: ActorBase = targets[0]

	##EFFECTS START
	##EFFECTS END
	var target_actions = target.action_holder.get_actions()
	var action : ActionBase = target_actions[randi_range(0, target_actions.size() - 1)]
	
	initiator.action_holder.add_action(AG.generate_action_by_name(action.code_name))
	

	var format_dict : Dictionary = {}
	format_dict["action.lore_name"] = action.lore_name

	return ActionResult.new(result_format, format_dict)
